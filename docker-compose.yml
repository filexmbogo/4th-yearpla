version: '3'
services:
  app:
    build: .
    ports:
      - "10000:10000"
    depends_on:
      - mongo

  mongo:
    image: mongo
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./data/db:/data/db
